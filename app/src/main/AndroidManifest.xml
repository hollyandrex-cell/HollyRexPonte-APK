<?xml version="1.0" encoding="utf-8"?>
<!-- MANIFEST AGGIORNATO PER NOVA FAMILY BUILD 20 - CORRETTO ICONA -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hollyandrex.pontevocale">

    <!-- Permessi per Ponte Vocale -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- ATTIVITA' PRINCIPALE - QUESTA FA COMPARIRE L'ICONA! -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servizi del Ponte -->
        <service
            android:name=".PonteService"
            android:exported="false" />

        <service
            android:name=".PonteNotificationService"
            android:exported="false" />

    </application>
</manifest>
